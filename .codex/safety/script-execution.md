# Execucao por script

Scripts futuros devem:

- validar ambiente;
- exigir filtros explicitos;
- imprimir resumo antes de executar;
- suportar preview;
- suportar dry-run;
- exigir confirmacao para envio real;
- bloquear envio real em local/dev sem autorizacao explicita;
- limitar lote;
- evitar destinatarios duplicados;
- registrar logs sem segredos.

Nenhum script executavel deve ser criado nesta etapa.
