# Task 03: desenhar client Mailgun

Objetivo futuro: desenhar `MailgunEmailProvider`.

Regras:

- usar `fetch` nativo;
- nao usar `axios`;
- bloquear envio real por padrao;
- mascarar segredos;
- suportar HTML e TXT.

Status: nao executada.
